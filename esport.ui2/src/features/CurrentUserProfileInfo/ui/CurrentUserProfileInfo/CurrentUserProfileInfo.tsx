import { FC } from "react";
import styles from "./CurrentUserProfileInfo.module.css";

interface CurrentUserProfileInfoProps {
  className?: string;
}

export const CurrentUserProfileInfo: FC<CurrentUserProfileInfoProps> = (props) => {

    return (
        <div className={styles.wrapper}>

        </div>
    );
};
