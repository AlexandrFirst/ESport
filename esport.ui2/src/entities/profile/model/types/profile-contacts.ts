interface Contact {
  title: string;
  link?: string;
}

export interface ProfileContacts {
  phone?: Contact;
  email?: Contact;
  skype?: Contact;
  facebook?: Contact;
  twitter?: Contact;
  youtube?: Contact;
  twitch?: Contact;
  instagram?: Contact;
  telegram?: Contact;
}
